# AI Interview Frontend

Next.js + TypeScript + Tailwind CSS front-end for the AI Interview Platform.

## Start

```bash
npm install
npm run dev
```

By default, `/api/*` is proxied to `http://127.0.0.1:8080`. Override it with:

```bash
BACKEND_API_URL=http://127.0.0.1:8080 npm run dev
```
